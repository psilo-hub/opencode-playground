---
description: Start or resume the one-question-at-a-time project discovery interview.
agent: orchestrator
---

Run or resume the project discovery interview:

1. Check `.opencode/state/project-interview.md` for prior progress; resume from there if present.
2. Delegate to the interviewer. Exactly one question at a time, concise answer options whenever practical, adapting to previous answers and repository evidence.
3. Persist progress after each answer so an interrupted interview can resume.
4. Stop once there is enough information for project initialization, initial module boundaries, architectural direction, and a first roadmap — not every future design detail.
5. On completion: update `PROJECT.md`, mark the interview complete, and report decided points plus remaining open questions.

Session input:

$ARGUMENTS
